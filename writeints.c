/**
 * Read integers from stdin and write them in binary form
 * to stdout. Redirect output to intsc.bin:
 * ...$ ./writeints.c >intsc.bin
*/
#include <stdio.h>

int main() {
    int i;
    while (scanf("%d", &i) != EOF){
        
    }
}